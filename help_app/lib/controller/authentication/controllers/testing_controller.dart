class TestingFareController {
  /*
  TestingFareController get find => Get.find();
  List<FareModel>? fareList;
  bool isLoaded = false;

  @override
  void onInit() {
    fetchData();
    _refresh();
    super.onInit;
  }

  fetchData() async {
    fareList = await RemoteApiService.fetchFare();
    if (fareList != null) {
      isLoaded = true;
    }
  }

  //Refresh page function
  Future<void> _refresh() async {
    await fetchData();
  }


*/
}
